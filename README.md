# Jisha-Enterprise-Sales-Aanalysis-by-sql
SQL-based analysis of sales data including data cleaning, exploration, and performance insights. Queries cover revenue trends, top products, regional performance, salesperson analysis, monthly summaries, and channel comparisons. A complete SQL script with key KPIs and analytics is included.


📘 Project Overview
This project — Jisha Enterprise Sales Analysis — focuses on exploring and analyzing the company’s sales data using MySQL.
The goal of this analysis is to uncover meaningful insights from the dataset, such as identifying top-performing regions, products, and salespersons, as well as analyzing sales channels and trends.
________________________________________
🧩 Steps Performed
1️⃣ Dataset Preparation
•	The original dataset was provided in Excel (.xlsx) format.
•	Converted the Excel file into CSV format for easier handling and database import.
2️⃣ Database Setup
•	Created a new database in MySQL Workbench.
•	Imported the converted CSV file (sales_data.csv) into MySQL.
•	Verified successful data import by running:
•	SELECT * FROM sales_data;
3️⃣ Data Cleaning
•	Checked for NULL values and missing data.
•	Performed data validation and ensured data types were consistent across all columns.
4️⃣ SQL Analysis and Queries
🟢 Basic Level Queries
1.	Display all sales records
2.	SELECT * FROM sales_data;
3.	Show unique products sold
4.	SELECT DISTINCT Product FROM sales_data;
5.	Find total number of sales transactions
6.	SELECT COUNT(*) AS Total_Transactions FROM sales_data;
7.	Retrieve all sales made in the ‘West’ region
8.	SELECT * FROM sales_data WHERE Region = 'West';
9.	Get all records where the sales channel is ‘Online’
10.	SELECT * FROM sales_data WHERE Channel = 'Online';
________________________________________
🟡 Intermediate Level Queries
6.	Find total revenue generated by each region
7.	SELECT Region, SUM(Revenue) AS Total_Revenue
8.	FROM sales_data
9.	GROUP BY Region
10.	ORDER BY Total_Revenue DESC;
11.	Calculate total units sold per product
12.	SELECT Product, SUM(Units_Sold) AS Total_Units
13.	FROM sales_data
14.	GROUP BY Product
15.	ORDER BY Total_Units DESC;
16.	Find average revenue per sale for each salesperson
17.	SELECT Salesperson, AVG(Revenue) AS Avg_Revenue
18.	FROM sales_data
19.	GROUP BY Salesperson
20.	ORDER BY Avg_Revenue DESC;
21.	Display top 5 highest revenue transactions
22.	SELECT *
23.	FROM sales_data
24.	ORDER BY Revenue DESC
25.	LIMIT 5;
26.	Count number of transactions for each customer type
SELECT Customer_Type, COUNT(*) AS Transaction_Count
FROM sales_data
GROUP BY Customer_Type;
________________________________________
🔵 Advanced Level Queries
11.	Find region generating the highest total revenue
SELECT Region, SUM(Revenue) AS Total_Revenue
FROM sales_data
GROUP BY Region
ORDER BY Total_Revenue DESC
LIMIT 1;
12.	Identify best-performing salesperson (highest total revenue)
SELECT Salesperson, SUM(Revenue) AS Total_Revenue
FROM sales_data
GROUP BY Salesperson
ORDER BY Total_Revenue DESC
LIMIT 1;
13.	Determine product with highest average revenue per unit sold
SELECT Product, AVG(Revenue / Units_Sold) AS Avg_Revenue_Per_Unit
FROM sales_data
GROUP BY Product
ORDER BY Avg_Revenue_Per_Unit DESC
LIMIT 1;
14.	Compare total Online vs Offline sales revenue
SELECT Channel, SUM(Revenue) AS Total_Revenue
FROM sales_data
GROUP BY Channel;
15.	Show monthly revenue trends for 2022
SELECT STRFTIME('%m', Date) AS Month, SUM(Revenue) AS Total_Revenue
FROM sales_data
GROUP BY Month
ORDER BY Month;
________________________________________
📊 Insights and Results
•	Identified top-performing regions and salespersons.
•	Compared online vs offline revenue performance.
•	Observed monthly sales trends for business growth evaluation.
•	Found most profitable products based on revenue per unit.
________________________________________
🛠️ Tools and Technologies
•	MySQL Workbench — Database Management and SQL Execution
•	Microsoft Excel — Data preparation and CSV conversion
•	CSV File Format — For easy data handling
•	SQL Queries — For analysis and insights
________________________________________
📁 Dataset
File: Sample Sales Data.csv
________________________________________

